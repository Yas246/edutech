# EduTech

Plateforme éducative — Bénin.

Projet Next.js (App Router) avec React, TypeScript, Tailwind CSS et PostgreSQL via Drizzle ORM.

## Démarrer

```bash
npm install
cp .env.example .env
npx drizzle-kit push
npm run seed
npm run dev
```

Ouvrir [http://localhost:3000](http://localhost:3000).
